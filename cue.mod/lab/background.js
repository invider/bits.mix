const Z = 0

function draw() {
    background(.1, .1, .2)

    fill(.5, .6, .8)
    font('20px zekton')
    alignCenter()
    baseMiddle()
    text('spawning cicles every 2s', rx(.5), ry(.8))
    text('and a pink one at 5s mark', rx(.5), ry(.85))
}
